name: Greetings

on: [pull_request, issues]

jobs:
  greeting: A-Shalobalo
    runs-on:ubuntu-latest
    steps:
    - uses: actions/first-interaction@v1
      with:
        repo-token: ${{ secrets.GITHUB_TOKEN }}
        issue-message: 'Hello, A-Shalobalo'
        pr-message: 'Your first pull request'
